import errorMiddlewares from './errorMiddlewares';
import validationMiddlewares from './validationMiddlewares';

const middlewares = {
    errorMiddlewares,
    validationMiddlewares,
};

export default middlewares;
